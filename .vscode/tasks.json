{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Docker Compose Up (Build)",
            "type": "shell",
            "command": "docker-compose up --build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
